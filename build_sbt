lazy val windows =
  project.in(file("windows")).
    settings(libSettings).
    settings(
      nativeVerbose := true,
      nativeClangOptions := Seq("-O2", "-lxcb", "-I/usr/include/xcb")
    ).
    dependsOn(scalalib)

